@model Aiursoft.MarkToHtml.Models.CategoryViewModels.DeleteCategoryViewModel

<h1 class="h3 mb-3">@Localizer["Delete Category"]</h1>

<div class="row">
    <div class="col-12 col-xl-6">
        <div class="card border-danger">
            <div class="card-header bg-danger-subtle">
                <h5 class="card-title mb-0 text-danger">
                    <i class="align-middle me-2" data-lucide="alert-triangle"></i>
                    @Localizer["Delete \"{0}\"?", Model.Name]
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-danger alert-outline mb-3" role="alert">
                    <div class="alert-icon">
                        <i class="align-middle" data-lucide="alert-octagon"></i>
                    </div>
                    <div class="alert-message">
                        <strong>@Localizer["This action is irreversible."]</strong>
                        @Localizer["All contents of this category will be permanently deleted."]
                    </div>
                </div>

                <h6>@Localizer["Contents of this category:"]</h6>
                <ul class="list-group mb-3">
                    @if (Model.DirectDocumentCount > 0)
                    {
                        <li class="list-group-item d-flex align-items-center gap-2">
                            <i class="align-middle text-muted" data-lucide="file-text"></i>
                            <span>@Localizer["{0} document(s)", Model.DirectDocumentCount]</span>
                        </li>
                    }
                    @if (Model.DirectSubCategoryCount > 0)
                    {
                        <li class="list-group-item d-flex align-items-center gap-2">
                            <i class="align-middle text-muted" data-lucide="folder"></i>
                            <span>@Localizer["{0} subcategory(ies)", Model.DirectSubCategoryCount]</span>
                        </li>
                    }

                    @if (Model.RecursiveDocumentCount > Model.DirectDocumentCount || Model.RecursiveSubCategoryCount > Model.DirectSubCategoryCount)
                    {
                        <li class="list-group-item d-flex align-items-center gap-2 bg-warning-subtle">
                            <i class="align-middle" data-lucide="folder-tree"></i>
                            <span>
                                <strong>@Localizer["Recursively:"]</strong>
                                @Localizer["{0} document(s) and {1} subcategory(ies) in total",
                                    Model.RecursiveDocumentCount, Model.RecursiveSubCategoryCount]
                            </span>
                        </li>
                    }

                    @if (Model.DirectDocumentCount == 0 && Model.DirectSubCategoryCount == 0)
                    {
                        <li class="list-group-item text-muted">
                            <i class="align-middle me-2" data-lucide="folder-open"></i>
                            @Localizer["This category is empty."]
                        </li>
                    }
                </ul>
            </div>
            <div class="card-footer text-end">
                <a asp-action="Index" asp-route-categoryId="@Model.ParentCategoryId" class="btn btn-light me-2">
                    @Localizer["Cancel"]
                </a>
                <form asp-action="DeleteCategory" method="post" class="d-inline">
                    <button type="submit" class="btn btn-danger">
                        <i class="align-middle me-2" data-lucide="trash-2"></i>
                        @Localizer["Delete Permanently"]
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>